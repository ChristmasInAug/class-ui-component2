<!--
═══════════════════════════════════════════════════════════════════════
 HANDOFF_TEMPLATE.md — 세션 인계 템플릿 (프로젝트 무관, 복사해서 사용)
═══════════════════════════════════════════════════════════════════════

이 파일은 "세션을 다음 사람/다음 세션에게 넘길 때" 쓰는 인계 문서 양식이다.
한 작업을 중단·전환할 때, 아래 한 파일에 현재 상태를 스냅샷으로 적는다.

■ 사용법 (둘 중 하나)
  1) 수동: 이 파일을 프로젝트 루트에 `.handoff.md`(또는 STATE.md)로 복사 → 아래 4구획을 채운다.
  2) 자동: handoff 스킬을 설치 → `/handoff` 호출 → 아래 형식으로 자동 생성·갱신.
     스킬 설치 방법: 이 파일과 함께 `.claude/skills/handoff/SKILL.md`를 새 프로젝트에 복사하면 된다.
     SKILL.md 본문에 이 문서의 실행 순서·형식을 그대로 옮겨 넣고, frontmatter만
     `name: handoff` / `description: ...`로 채우면 별도 설정 없이 `/handoff`로 호출된다.

■ 핵심 원칙 (이게 인계 문서의 가치를 만든다)
  - ✅ 는 "검증된 것만". 각 항목에 증거(`실행한 명령` · `파일:라인` · 테스트 결과)를 붙인다.
    증거가 없으면 ✅ 가 아니다 → ⏳ 로 내린다. "되겠지"로 완료 표시 금지.
  - ❌ 는 "시도했지만 안 된 접근 + 원인". 다음 세션이 같은 막다른 길을 반복하지 않게.
  - ⏳ 는 "바로 이어갈 다음 단계 + 시작점(파일/명령)". 미착수 항목도 빠짐없이.
  - 이어받는 사람 관점으로 쓴다: "내가 뭘 했나"가 아니라 "다음 사람이 뭘 알아야 이어가나".
  - 간결·스캔 가능하게. 각 항목 한 줄. 산문 단락 금지.
  - 기존 인계 파일이 있으면 덮어쓰지 말고 갱신: 끝난 항목은 ✅로, 유효한 미완료는 ⏳로 carry-forward.
  - 이 파일 자체가 민감정보를 담을 수 있다는 것을 잊지 말 것. 인계 내용에는 종종 실제 작업
    맥락(내부 URL, 진행 중인 이슈, 때로는 대화에서 나온 실제 텍스트)이 그대로 들어간다.
    저장소가 PUBLIC이라면 인계 파일(`.handoff.md`/`STATE.md`)을 `.gitignore`에 넣어
    git 추적에서 제외한다 — 커밋해서 푸시하는 순간 그 내용이 그대로 공개된다.

■ 컨텍스트 수집 (채우기 전에 실제로 실행 — 추측 금지)
  date '+%Y-%m-%d %H:%M'
  git branch --show-current
  git status --short        # 신규/untracked 포함 변경 요약
  git diff --stat           # tracked 변경 요약
  # + 이번 세션에서 돌린 빌드/테스트/실행 명령의 실제 출력

──────────── 아래부터가 실제 템플릿. 사용 시 이 주석 블록은 지운다. ────────────
-->

# 🤝 Handoff — <한 줄 현재 상태 요약>

> 갱신: <YYYY-MM-DD HH:MM> · 브랜치: `<branch>` · 변경: `<git status --short 요약 (신규/untracked 포함) — tracked 변경은 git diff --stat>`

## ✅ 완료 (검증된 것만)
<!-- 증거 없는 항목은 여기 두지 말 것. 비었으면 `- 없음` -->
- [x] <한 일> — 증거: `<실행한 명령 / 파일:라인 / 테스트 결과>`
- [x] <한 일> — 증거: `<…>`

## ❌ 시도 → 실패 / 막힘 (반복 금지)
<!-- 시도한 접근 + 왜 안 됐는지 원인. 비었으면 `- 없음` -->
- [ ] <시도한 접근> — 원인: <왜 안 됐나> → 다음 대안: <있으면>

## ⏳ 다음 할 일 / 미착수
<!-- 바로 이어갈 다음 단계. 시작점(파일/명령)을 함께. -->
- [ ] <다음 단계> — 시작점: `<파일 / 명령>`
- [ ] <아직 손 안 댄 것>

## 📌 컨텍스트 메모
<!-- 이어받는 사람이 모르면 헤맬 것: 결정·제약·함정·환경 설정. 비었으면 생략 가능 -->
- <결정/제약/주의 한 줄>
- <환경·합의된 방향·알아둘 함정>

<!--
═══════════════════════════════════════════════════════════════════════
 예시 (작성 감 잡기용 — 실제 사용 시 삭제)
═══════════════════════════════════════════════════════════════════════

# 🤝 Handoff — GitHub Pages 게시 완료(자동배포 확인됨), 브라우저 수동 검증만 남음

> 갱신: 2026-07-30 06:01 · 브랜치: `feature/ui` · 변경: 작업 트리 clean — 로컬 main은 원격보다 뒤처짐

## ✅ 완료 (검증된 것만)
- [x] GitHub Pages 게시 — .gitignore로 민감 파일 추적 중단, 배포 워크플로 추가, PR merge —
      증거: `gh run watch`로 전 단계 성공 확인, `curl` 결과 root 200 / 내부 파일 404
- [x] main push 시 자동 배포 확인 — 증거: PR merge(=push 이벤트)가 워크플로를 자동 트리거함을
      `gh run list --json event,headBranch,conclusion`로 확인

## ❌ 시도 → 실패 / 막힘 (반복 금지)
- 없음

## ⏳ 다음 할 일 / 미착수
- [ ] 브라우저 수동 검증(사이드바 클릭, 탭 전환, 모달류 열고 닫기, 복사 버튼) —
      시작점: 배포된 URL 또는 로컬 파일을 브라우저로 직접 열기
- [ ] 로컬 main을 원격과 동기화 — 시작점: `git checkout main && git pull origin main`

## 📌 컨텍스트 메모
- Pages 배포는 main push에 완전 자동화됨(`.github/workflows/deploy-pages.yml`), `_site`에는
  앱 파일만 복사되어 내부 문서·로그 파일은 노출되지 않음(확인됨).
-->
